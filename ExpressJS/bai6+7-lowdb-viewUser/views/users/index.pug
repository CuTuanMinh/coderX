h1 Users

a(href="/users/create") New

br

form(action="/users/search", method="GET")
	input(type="text", name="q", value=param)
	button Search

ul
	each user in users
		li
			span= user.name
			span &nbsp
			a(href="/users/" + user.id) View
